# Add Node.js memory limit for HF Spaces
NODE_OPTIONS=--max-old-space-size=2048

# API URL for frontend to connect to backend (empty for relative paths in HF Spaces)
NEXT_PUBLIC_API_URL=

# Environment
NODE_ENV=production
